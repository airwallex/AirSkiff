group=com.airwallex.airskiff
name=core
version=2.0.64
org.gradle.daemon=true
org.gradle.parallel=true
file.encoding=utf-8

# Token-based authentication for publishing
# These properties are used as fallbacks if environment variables are not set
ossrhUsername=your-token-username
ossrhPassword=your-token-password
